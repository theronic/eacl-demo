# Canonical fixtures

Deterministic schema, stable IDs, generator/version seed, 10,000-resource semantic prefix, 1,000,000-resource target, exemplars, counts, relationships, and golden digests.

Durable seed execution is not located here; private stateful workflows consume accepted fixture manifests.
