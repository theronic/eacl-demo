# Verification

Initial/material-change qualification; manually dispatched browser,
accessibility, bounded load, exact-version memory, closed fault, seed,
migration, and rollback workflows; and the deliberately small merge smoke
suite. Manual runtime and transition evidence is written under
`verification/results/` and is never inferred from a workflow definition.
Formal verification remains independent and non-gating for demo deployments.
