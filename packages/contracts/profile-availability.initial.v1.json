{
  "$schema": "../../schemas/profile-availability.v1.schema.json",
  "schema": "eacl-demo.profile-availability.v1",
  "profiles": [
    {"id": "datahike-s3", "state": "qualifying", "reason": "The adopted store and replacement reader are locally qualified, but the live Lambda route, immutable deployment identity, memory, and staged smoke remain unqualified."},
    {"id": "datahike-dynamodb", "state": "unavailable", "reason": "The repaired adapter passes local qualification, but disposable real-AWS evidence, the protected production table, live cost controls, the one-million-resource seed, memory, and staged smoke remain open."},
    {"id": "datomic-dynamodb", "state": "disabled", "reason": "The history-preserving DynamoDB database and fixed-current read-only Lambda have not been provisioned and qualified."},
    {"id": "datalevin-memory", "state": "unavailable", "reason": "A reproducible AL2023 arm64 native candidate now passes ABI and embedded LMDB smoke locally, but the maintained fork/native/EACL releases, clean remote-consumer install, Lambda service, and SnapStart lifecycle qualification remain incomplete."},
    {"id": "jank-memory", "state": "unavailable", "reason": "Parked outside the active rollout; the vendored runtime port also predates the locked EACL Core SHA and no qualified Linux x86_64 Amazon Linux 2023 artifact exists."},
    {"id": "datascript-browser-memory", "state": "qualifying", "reason": "The direct EACL v8 browser runtime and canonical fixture pass local build and isolation checks, but trusted staged browser qualification and publication remain open."}
  ]
}
